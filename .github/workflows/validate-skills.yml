name: Validate Skills

on:
  pull_request:
    paths:
      - 'curated/**'
      - 'community/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 校验所有技能格式
        run: bash scripts/validate-skill.sh
